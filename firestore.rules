rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DIAGNOSTIC: Open rules to everyone to isolate the "insufficient permissions" error
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
