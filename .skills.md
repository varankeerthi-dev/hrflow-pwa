🧠 Core Principle
Always analyze existing codebase first
Do NOT introduce new frameworks, libraries, or patterns
Follow current architecture, conventions, and folder structure strictly
Prefer consistency over improvement
🔍 Code Understanding
Read:
Existing components
API structure
State management patterns
Styling approach (CSS / Tailwind / inline)
Identify:
Naming conventions
File structure patterns
Reusable utilities
Extend — never rewrite unless explicitly asked
⚙️ Tech Stack Adherence
Use only:
Existing frameworks (e.g., React, Laravel, Supabase)
Existing libraries already installed
❌ Do NOT:
Add new npm packages
Replace existing tools (e.g., don’t replace fetch with axios)
Introduce new state managers (Redux, Zustand, etc.)
🧩 Frontend Rules (React / Vite / TSX)
Follow existing component patterns:
Functional components
Hooks already in use
State:
Use same pattern (useState / context / existing query logic)
Data fetching:
Follow current approach (fetch / existing abstraction)
Do NOT introduce TanStack Query unless already used
Styling:
Use same system (Tailwind / CSS modules / inline)
UI:
Match spacing, font, color, and structure exactly
🔗 API & Data Handling
Follow existing API structure:
Endpoints
Request format
Response handling
Maintain:
Same error handling pattern
Same validation logic
Do NOT:
Change API contracts
Introduce new backend logic unless requested
🗂️ File & Folder Rules
Place files:
In existing folders only
Following current naming conventions
Do NOT:
Create new architectural layers
Restructure project
🧪 Debugging & Fixes
Fix within current logic
Minimal changes only
Preserve:
Existing behavior
Existing UI
Avoid refactoring unless explicitly requested
🧱 Database & Logic Rules
Follow existing schema
Respect:
Business logic
Conditions (e.g., stock deduction, approval flows)
Example:
If stock = 0 → mark as "Out of Stock"
If stock < threshold → show "Low Stock"
Do NOT:
Change logic flow
Introduce new calculations
🎯 Output Expectations
Code should:
Blend seamlessly with existing code
Look like written by original developer
Avoid:
Overengineering
Unnecessary abstraction
🚫 Strict Prohibitions
❌ No new frameworks
❌ No new libraries
❌ No architecture changes
❌ No UI redesign unless asked
❌ No optimization that changes behavior
✅ Preferred Approach
Read existing code
Identify pattern
Replicate pattern
Implement minimal solution
Keep everything consistent